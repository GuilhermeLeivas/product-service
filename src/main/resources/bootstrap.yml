---
eureka:
  client:
    service-url:
      default-zone: ${EUREKA_URL:localhost}:${EUREKA_PORT:8761}
---
eureka:
  instance:
    prefer-ip-address: true
---
spring:
  application:
    name: product-service